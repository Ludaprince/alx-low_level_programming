#include "main.h"
/**
 * add - add two numbers together
 * @a: the first number tonbe added
 * @b: seconf number to be added
 *
 * Return: return 0 if successful
 */
int add(int a, int b)
{
	return (a + b);
}
