#include <stdio.h>
/**
 * main - printing of lowercase letters except q and e
 * Return: Always 0
 */
nt main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		else
		{
		}
	}
	putchar('\n');
	return (0);
}
